# Knowledge Gateway 与证据协议

当回答依赖公司历史、业务规则、产品行为、指标口径、系统约束或历史决策时，完整执行本协议。

## 一、能力契约

开源 Skill 不绑定 Confluence、飞书、Notion、Google Drive 或任何单一厂商。优先使用提供以下能力的 Knowledge Gateway MCP：

### `get_knowledge_profile`

返回当前组织、员工账号、权限模式、是否只读和已连接账号。

### `list_knowledge_sources`

返回当前用户可访问的知识源，至少包含 `source_id`、`name`、`type`、`organization`、`permission_mode` 和 `read_only`。

### `search_knowledge`

接受：

- `query`：精确检索语句
- `source_ids`：知识源范围
- `document_types`：PRD、决策、会议纪要、工单、规则、接口、数据口径等
- `date_from` / `date_to`：可选时间范围
- `limit`：结果数量上限

每个结果应尽可能返回 `document_id`、标题、命中摘要、文档类型、状态、负责人、更新时间、来源和用户可访问链接。

### `read_knowledge_item`

根据 `document_id` 打开原文，返回：

- 命中段落及必要上下文
- 章节名称
- 版本和文档状态
- 创建、更新或生效日期
- 作者或规则负责人
- 适用用户、业务、地区或系统版本
- 原文链接
- 是否存在权限截断或内容缺失

如果工具名称不同，但能完成上述能力，将返回结果归一化为同等字段后继续。

## 二、主动检索条件

出现以下任一情况时主动查询：

- 用户明确要求查询历史资料。
- PM 不清楚规则来源、历史原因或现在是否仍适用。
- PRD 包含“沿用原逻辑”“与线上一致”“默认按历史处理”等表述。
- 参会者询问存量用户、历史数据、旧版兼容或过往承诺。
- 不同人员或文档对同一规则的表述不一致。
- 新需求可能影响合同、法规、客户承诺、运营流程或存量数据。
- 当前 PRD 与线上行为不一致。

## 三、检索 SOP

1. 把会议问题转成一个可验证的精确主张，例如“存量会员是否必须重新授权自动续费”。
2. 获取当前知识账号和可用知识源；连接器已授权时无需重复询问。
3. 提取产品名、业务对象、状态名、规则术语、旧名称、工单编号、负责人、日期和版本。
4. 先查当前有效的正式规则、决策记录和当前 PRD。
5. 再查历史 PRD、会议纪要、工单和运营手册。
6. 必要时核对当前系统的代码、配置、数据或受控观察结果。
7. 打开原始文档，检查上下文、文档状态、版本、负责人、生效日期和适用范围。
8. 高影响或有争议的问题至少交叉验证两个信息源。
9. 记录信息源、结论摘要、证据状态、适用范围和未解决冲突。
10. 当会议时间内继续检索不太可能改变结论时，停止搜索并创建待确认项。

## 四、信息源优先级

1. 当前已批准的政策、合同、法规或具名决策记录。
2. 当前版本 PRD、负责人维护的规则库、接口或数据文档。
3. 通过代码、配置、数据或受控观察验证的已上线行为。
4. 带明确负责人的近期会议记录、工单和运营手册。
5. 数据分析、客服案例和用户研究。
6. 个人回忆、聊天片段、推测的原意和无文档惯例。

当前系统行为只能证明“现在如何运作”，不能自动证明“应该如何运作”。

## 五、证据记录

| 字段 | 内容 |
|---|---|
| 待验证主张 | 本次究竟要确认什么 |
| 当前结论 | 现有证据支持的结论 |
| 证据状态 | 已确认 / 有据推断 / 存在冲突 / 未知 |
| 来源 | 文档名、章节、可访问链接或位置 |
| 版本与日期 | 文档版本、生效日期或更新时间 |
| 负责人 | 文档或规则负责人，不明时写未知 |
| 适用范围 | 适用用户、地区、业务、渠道或版本 |
| 未解问题 | 还缺哪项证据或谁需确认 |

## 六、冲突处理

信息源冲突时，输出：有争议的主张、各信息源的版本与生效日期、当前实际行为、受影响用户或系统、可逆与不可逆后果、有权决定的负责人，以及建议补充的证据或待决事项。不得默默选择对当前方案最有利的信息源。

## 七、失败与降级

### 未找到资料

- 不得将“没搜到”说成“公司没有这条规则”。
- 尝试同义词、产品旧名、历史状态名、工单号和负责人。
- 列出已查询的来源和时间范围。
- 标记为“未知”并生成人工待确认项。

### 资料过时

- 仅作为历史背景，不直接认定仍然有效。
- 标出最后更新时间和可能失效原因。

### 无连接器或无权限

- 明确说明哪个来源不可用，不得暗示已搜索。
- 请用户连接相应知识库，或提供文档、链接或导出文件。
- 继续完成不依赖该知识源的评审内容。

## 八、会中时间控制

**快速检索**：优先当前正式规则、当前 PRD 和具名决策记录；返回一至三句建议回答和简短来源；无法快速确认时立即标记“待确认”。

**会后深度追溯**：扩展查询历史 PRD、会议纪要、工单、代码、配置和数据，建立规则时间线，判断产生原因和当前适用性。

不得为了继续搜索让会议长时间停滞。

## 九、安全与权限

- Knowledge Gateway 使用员工本人身份，并在服务端每次请求时校验权限。
- 默认只读，不提供写入、删除或权限变更能力。
- 不将令牌、密钥或密码传入对话、文档结果或普通日志。
- 只返回完成当前任务所需的片段和元数据。
- 保留原文档权限边界，不因连接器聚合而扩大可见范围。
